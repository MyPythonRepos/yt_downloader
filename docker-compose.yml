services:
  yt_downloader:
    container_name: yt_downloader
    image: cleondock/yt_downloader:0.0.1
    restart: unless-stopped
    environment:
      TZ: 'Europe/Madrid'
    ports:
      - "5000:5000/tcp"
    volumes:
      - './downloads:/downloads'
    
